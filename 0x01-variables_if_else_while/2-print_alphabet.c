#include <stdio.h>

/**
 * main - print the alphabets in lowercase.
 *
 * Return: Always 0
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letters);

	putchar('\n');

	return (0);
