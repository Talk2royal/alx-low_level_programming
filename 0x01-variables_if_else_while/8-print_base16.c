#include <stdio.h>

/**
 * main - print all the numbers of base 16 in lowercase.
 *
 * Return: Always 0
 */
int main(void)
{
	int n;
	char ch;

	for (n = 48; num <= 56; n++)
	{
		putchar(num);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}

	putchar('\n');

	return (0);
}
